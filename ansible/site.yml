---
- name: 共通設定 + セキュリティ強化
  hosts: all
  roles:
    - common
    - security
    - dev_user

- name: NFS Server 構成
  hosts: nfs_servers
  roles:
    - nfs_server
    - samba_timemachine

- name: AI Commander 構成
  hosts: ai_commanders
  roles:
    - cli_tools
    - mosh
    - nfs_client
    - claude_code
